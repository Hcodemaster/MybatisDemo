spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/students?useSSL=false&useUnicode=true&characterEncoding=utf8
    username: root
    password: password
mybatis:
  mapper-locations:
    - classpath:mybatis/mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true